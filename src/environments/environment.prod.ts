export const environment = {
  production: true,
  // TODO: replace with the actual URL Render assigns to the "server/" web
  // service once it's created (Render dashboard -> that service -> URL at
  // the top of the page), then rebuild/redeploy the static site.
  chatApiUrl: 'https://asfand-portfolio-chat-api.onrender.com/api/chat'
};
