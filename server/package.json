{
  "name": "portfolio-chat-server",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal proxy that attaches the portfolio's system prompt and calls the Gemini API server-side, so the API key never reaches the browser.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.1"
  }
}
